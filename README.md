# ART_NARA

ART_NARA is organized as two separately deployable applications:

- `frontend`: Flutter client application
- `backend`: Spring Boot API server

The initial implementation is based on the technology patterns and domain structure from the Knot reference application. Features will be implemented incrementally from the Figma wireframes, with frontend and backend changes kept separately reviewable.
